===================================================================
WEEK 1 / DAY 3: TASK 1.5 - MEDALLION IMPLEMENTATION NOTES
===================================================================

1. BRONZE-TO-SILVER TEMPLATE RULE:
   - Implementation Lesson: Always enforce programmatic string datetime parsing and row-count sanity filtering at the immediate entry point of the Silver layer.
   - Reusable Component: Filter templates targeting invalid business records (e.g., passengerCount > 0, tripDistance > 0) should be abstracted into a core cleaning class library.

2. SILVER-TO-GOLD REUSE SCHEMA:
   - Implementation Lesson: Gold layer transformations must leverage pre-aggregated Delta Lake materialized view logic to prevent slow, repetitive on-the-fly table reads.
   - Reusable Component: Standardized timestamp ingestion tracking columns (`ingestion_timestamp`) must be appended globally across all production tables to preserve lineage history.

3. DELTA STORAGE OPTIMIZATION RULE:
   - Implementation Lesson: Enforce explicit V-Order optimization configurations during all Gold write sequences to supercharge downstream Power BI reporting caches natively out of storage.
